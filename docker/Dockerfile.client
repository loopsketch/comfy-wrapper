FROM python:3.12-slim

# ffmpeg は postprocess.py が入力クリップの寸法と fps を ffprobe で読むのに使う
RUN apt-get update \
    && apt-get install -y --no-install-recommends ffmpeg \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
